# GitHub Token for npm publishing to GitHub Packages
# Required for: npm publish to GitHub npm registry
# Note: GitHub Actions automatically provides GITHUB_TOKEN, no setup needed there
# For local publishing, create a GitHub Personal Access Token with 'write:packages' scope
# https://github.com/settings/tokens/new
GITHUB_TOKEN=your_github_token_here

# (Optional) Default Ollama API URL for development/testing
# If not set, the OllamaClient uses http://localhost:11434/api
OLLAMA_BASE_URL=http://localhost:11434/api
